[package]
name = "bulks"
version = "0.1.0"
edition = "2024"

[features]
alloc = []

[dependencies]
moddef = "0.3.0"
currying = {version = "0.4.0", default-features = false, features = ["8"]}
array_trait = {version = "1.1.2", default-features = false}